# Embedded mode: SQLite only, no PostgreSQL container.
# Use this for Conductor integration or standalone deployment.
#
# Usage: docker compose -f docker-compose.embedded.yml up

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
      additional_contexts:
        certs: ./certs
    container_name: andy-issues-embedded
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=https://+:8443;http://+:8080
      - Database__Provider=Sqlite
      - ConnectionStrings__DefaultConnection=Data Source=/data/andy_issues.db
      - AndyAuth__Authority=https://host.docker.internal:5001
      - AndyAuth__Audience=urn:andy-issues-api
      - Rbac__ApiBaseUrl=https://host.docker.internal:5003
      - Rbac__ApplicationCode=andy-issues
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "5410:8443"
      - "5411:8080"
    volumes:
      - ./certs:/usr/local/share/ca-certificates/custom:ro
      - sqlite_data:/data
      - dpkeys:/app/.aspnet/DataProtection-Keys
    healthcheck:
      test: ["CMD", "curl", "-fk", "https://localhost:8443/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 15s
    restart: unless-stopped

volumes:
  dpkeys:
  sqlite_data:
