# Triage reconciliation — 2026-09-08

The open triage parents #108 and #109 contain stale unchecked and truncated descriptions. Their child implementations are broader than those descriptions suggest, but closing the parents would still overstate cross-service completion.

| Area | Verified implementation | Remaining boundary |
| --- | --- | --- |
| State, dispatch, schema, events | `IssueService`, `TriageController`, `IssueEvents`, schema and pipeline tests; #110–#113 | Deployment wiring |
| Detail and review | Triage output editing, revisions and revert endpoints; #114 | Full embedded Conductor verification |
| Audit references | PR #219: immutable run inputs, output DocsRef, run correlation, metadata event fields and atomic persistence | Conductor #2354 Docs route plus live Tasks/Docs audit verification |
| Estimator | #116 cold-start USD/hour baselines with severity multiplier | The explicitly deferred learned model needs tenant/completion-count contract; it is not silently marked delivered |
| Attachments and planning | #117 plus Tasks PR #415: actual producer fixture → accepted event → Goal → planning audit, preserving inputs, owner and run; hours converted to seconds | Final deployment verification |
| MCP, CLI, OpenAPI, docs | `ServiceTools`, `IssuesCommand`, `TriageOpenApiTests`, lifecycle ADR, #119–#123 | None found in this review |

Tasks PR #415 fixes the incompatible old flattened consumer, maps the actual enum classification to the workflow seeds, gates current events on human acceptance, prevents replay duplicates and preserves initial-estimate provenance. Its fixture was generated by the actual Issues serializer. Local verification: 1,829 unit and 388 integration passes; hosted checks include PostgreSQL and NATS.

Conductor PR #2354 has 65 passing service-host package tests and strict lint, but full app CI fails before compilation while checking out private `swift-ide-components` with a rejected credential in the `dev` environment. Renew that credential before validating and merging the companion. Do not treat passing package tests as a full embedded deployment result. The same credential blocks #2352, which must merge before Issues' incompatible async recategorization API #196.

The raw AI-config endpoint #93 is a separate authorization hold; only encrypted persistence has merged. Shared viewer primitives and navigation list semantics are tracked under #107 and upstream Andy UI issues 2, 36 and 37.
